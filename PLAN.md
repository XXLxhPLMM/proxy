# 项目重构计划

## 项目概述
本次重构旨在优化代理服务器的架构设计，提升系统的可维护性、扩展性和管理能力。

---

## 任务列表

### 1. 修改服务器环境参数加载逻辑

**优先级**: 高
**预计工作量**: 2-3小时

**当前问题**:
- 环境参数加载分散，缺乏统一管理
- 配置项之间可能存在依赖关系未处理
- 缺少配置验证机制

**改进目标**:
- 建立统一的配置加载中心
- 支持配置验证和默认值处理
- 实现配置热加载功能
- 添加配置文档化注释

**具体任务**:
- [ ] 创建统一的配置加载类 `ConfigLoader`
- [ ] 实现配置项依赖关系解析
- [ ] 添加配置验证函数
- [ ] 实现配置文件格式化输出
- [ ] 更新所有配置引用点
- [ ] 编写配置加载单元测试

**输出文件**:
- `src/config/loader.ts` - 统一配置加载器
- `src/config/validator.ts` - 配置验证器
- `src/config/types.ts` - 配置类型定义

---

### 2. 架构重构：Intermediary 改为客户端模式

**优先级**: 高
**预计工作量**: 3-4小时

**当前问题**:
- Intermediary 模式职责不清晰
- 与 Client 模式功能重叠
- 缺乏多协议支持

**改进目标**:
- 将 Intermediary 重构为 Agent 模式
- Agent 和 Client 同属客户端模式
- 统一多协议处理架构

**具体任务**:
- [ ] 重构 Intermediary 模块为 Agent 模块
- [ ] 统一客户端接口设计
- [ ] 实现协议抽象层
- [ ] 更新路由配置
- [ ] 修改启动脚本
- [ ] 更新文档和注释

**输出文件**:
- `src/agent/index.ts` - Agent 模块入口
- `src/agent/protocol-handler.ts` - 协议处理器
- `src/client/protocol-manager.ts` - 协议管理器

---

### 3. SOCKS 代理协议支持

**优先级**: 高
**预计工作量**: 4-5小时

**当前问题**:
- 仅支持 HTTP/HTTPS/TCP 代理
- 缺少 SOCKS5 协议支持

**改进目标**:
- 实现 SOCKS5 代理协议
- 支持身份验证
- 实现协议转换

**具体任务**:
- [ ] 设计 SOCKS5 协议数据包结构
- [ ] 实现 SOCKS5 协议解析器
- [ ] 实现 SOCKS5 协议封装器
- [ ] 添加身份验证支持
- [ ] 集成到代理服务器
- [ ] 编写协议测试用例

**输出文件**:
- `src/protocols/socks5.ts` - SOCKS5 协议实现
- `src/protocols/base.ts` - 协议基类
- `src/protocols/http.ts` - HTTP 协议实现
- `src/protocols/https.ts` - HTTPS 协议实现

---

### 4. 优化管理核心控制器

**优先级**: 高
**预计工作量**: 5-6小时

**当前问题**:
- 管理核心与服务器强耦合
- 无法独立运行或导入
- 参数修改不灵活

**改进目标**:
- 实现管理核心独立运行
- 支持从其他进程导入运行
- 提供完整的参数管理接口
- 实现动态参数修改

**具体任务**:
- [ ] 设计管理核心独立架构
- [ ] 实现进程间通信机制
- [ ] 创建参数管理 API
- [ ] 实现动态配置更新
- [ ] 实现运行时控制接口
- [ ] 添加权限验证
- [ ] 编写管理接口文档

**输出文件**:
- `src/manager/core.ts` - 管理核心
- `src/manager/api.ts` - 管理 API
- `src/manager/process-manager.ts` - 进程管理
- `src/manager/permission.ts` - 权限控制

---

### 5. TUI 模式和 Web 模式集成

**优先级**: 中
**预计工作量**: 4-5小时

**当前问题**:
- TUI 和 Web 模式独立实现
- 缺乏统一的管理接口
- 无法实时交互

**改进目标**:
- 统一管理界面架构
- 实现实时通信
- 支持踢人、禁用 IP 等操作
- 提供图形化配置界面

**具体任务**:
- [ ] 设计统一管理界面架构
- [ ] 实现 WebSocket 通信
- [ ] 实现用户管理功能
- [ ] 实现代理管理功能
- [ ] 实现路由管理功能
- [ ] 实现 IP 禁用/启用
- [ ] 实现用户踢出功能
- [ ] 开发 TUI 界面
- [ ] 开发 Web 界面

**输出文件**:
- `src/ui/socket.ts` - WebSocket 服务
- `src/ui/tui.ts` - TUI 界面
- `src/ui/web.ts` - Web 界面
- `src/ui/types.ts` - 界面类型定义

---

### 6. 有状态和无状态模式抽象

**优先级**: 中
**预计工作量**: 4-5小时

**当前问题**:
- 状态管理逻辑分散
- 缓存层和参数层耦合
- 缺乏统一抽象

**改进目标**:
- 抽象服务参数层
- 抽象缓存层
- 支持状态模式切换
- 实现缓存策略统一

**具体任务**:
- [ ] 设计参数抽象层接口
- [ ] 设计缓存抽象层接口
- [ ] 实现有状态模式
- [ ] 实现无状态模式
- [ ] 实现模式切换机制
- [ ] 实现缓存策略
- [ ] 编写抽象层测试

**输出文件**:
- `src/core/state.ts` - 状态管理
- `src/core/cache.ts` - 缓存抽象层
- `src/core/params.ts` - 参数抽象层
- `src/core/types.ts` - 核心类型定义

---

## 实施顺序

### 阶段一：基础架构优化（第1-2周）
1. 任务 1: 修改服务器环境参数加载逻辑
2. 任务 4: 优化管理核心控制器
3. 任务 6: 有状态和无状态模式抽象

### 阶段二：协议扩展（第3-4周）
4. 任务 2: Intermediary 改为客户端模式
5. 任务 3: SOCKS 代理协议支持

### 阶段三：管理界面开发（第5周）
6. 任务 5: TUI 模式和 Web 模式集成

---

## 技术要点

### 协议处理
- 使用适配器模式处理不同协议
- 实现协议层抽象基类
- 支持协议扩展

### 管理架构
- 采用微服务思想设计管理核心
- 使用 WebSocket 实现实时通信
- 采用观察者模式实现状态同步

### 状态管理
- 状态模式区分有状态和无状态
- 策略模式实现不同缓存策略
- 工厂模式创建不同服务实例

### 进程通信
- 使用 IPC (Inter-Process Communication)
- 实现消息序列化
- 添加通信安全性验证

---

## 测试要求

### 单元测试
- 每个模块必须有对应的单元测试
- 测试覆盖率不低于 80%

### 集成测试
- 管理核心与服务器集成测试
- 多协议互通测试
- 状态切换测试

### 性能测试
- 配置加载性能测试
- 协议处理性能测试
- 管理接口响应时间测试

---

## 风险点

1. **架构变更风险**: 需要确保现有功能不受影响
2. **协议兼容性**: 新增协议需要向后兼容
3. **性能影响**: 重构可能影响系统性能
4. **测试覆盖**: 需要确保测试充分

---

## 备注

- 每完成一个任务需要更新对应的文档
- 代码审查必须严格执行
- 定期进行代码重构和优化
- 保持代码风格一致性

---

**创建时间**: 2026-05-26
**预计完成时间**: 5周
**状态**: 待开始
